<div id="image-container">


		<% @pictures.each do |picture| %>
	<div class="box">
	<div class="pic-info"><%= picture[:title] %> , <%= picture[:artist] %></div>
	<div class="image"><%= link_to image_tag(picture.url), "/pictures/#{picture.id}"%></div>
	</div>
		<% end %>


</div>